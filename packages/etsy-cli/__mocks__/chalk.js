/* eslint-disable no-undef */
const chalk = {
  red: (str) => str,
  green: (str) => str,
  blue: (str) => str,
  yellow: (str) => str,
  bold: (str) => str,
  dim: (str) => str,
  cyan: (str) => str,
  magenta: (str) => str,
  white: (str) => str,
  gray: (str) => str,
};

module.exports = chalk;
module.exports.default = chalk;
